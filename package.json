{
  "name": "docsvault",
  "version": "0.1.0",
  "description": "Web-Chat UI für otris DOCUMENTS Dokumentation",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "dev:codex": "BRIDGE=codex node --watch src/server.js",
    "dev:claude": "BRIDGE=claude node --watch src/server.js",
    "crawl": "node crawl.mjs",
    "crawl:login": "node crawl.mjs --login",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.81",
    "@modelcontextprotocol/sdk": "^1.27.0",
    "@openai/codex-sdk": "^0.116.0",
    "express": "^5.1.0",
    "ws": "^8.18.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
